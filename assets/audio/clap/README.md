# Clap sound

`clap.wav` is the applause file used when a mystery box opens. It includes a short lead-in so the applause aligns with the prize reveal animation.

To replace it, upload another WAV/MP3 file here and update `GAME_CONFIG.clapSound.file` in `index.html`. Keeping the same filename requires no code change.
